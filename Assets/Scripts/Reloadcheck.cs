using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Reloadcheck : MonoBehaviour
{
    // Start is called before the first frame update
    public void reload()
    {
        Muzzletest.check = true;
    }
}
